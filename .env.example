# Used only by scripts/discover.ts (bun run discover). Copy to .env and fill in.
EUFY_EMAIL=
EUFY_PASSWORD=
# Optional. A fixed value avoids creating a new Eufy app session on every run.
# EUFY_OPENUDID=
